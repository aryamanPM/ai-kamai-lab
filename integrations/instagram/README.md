# Instagram integration

This module is the boundary between AI Kamai Lab and Meta's supported Instagram APIs.

Required runtime secrets will be supplied through the deployment environment, never committed to Git.

Expected configuration:
- INSTAGRAM_ACCESS_TOKEN
- INSTAGRAM_USER_ID
- INSTAGRAM_API_VERSION

Publishing is intentionally disabled until valid credentials are configured and an explicit approval gate is passed.
